//
//  SpellingWord.swift
//  WordsForest
//
//  Created by Nami .T on 2025/11/04.
//

/*import Foundation 📄このファイルはSpellingModels.swiftに統合されました

struct SpellingWord: Identifiable, Hashable {
    let id = UUID()
    let text: String           // 英単語（ゲーム用に大文字でもOK）
    let meaningJa: String      // 日本語
    let pos: PartOfSpeech      // 既存のPOS

    init(card: WordCard) {
        self.text = card.word
        self.meaningJa = card.meaning
        self.pos = card.pos
    }
}
*/
